from config import base_url


class Urls:

    dresses_category_url = base_url + '?id_category=8&controller=category'
    authentication_pg_url = base_url + '?controller=authentication&back=my-account'
    account_url = base_url + '?controller=my-account'
